<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <!-- FrameLayout for content + FAB -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <RelativeLayout
            android:id="@+id/main"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical">

            <!-- Toolbar -->
            <include
                android:id="@+id/includeToolbar"
                layout="@layout/toolbar"/>

            <!-- Greeting Text -->
            <TextView
                android:id="@+id/greetingTextOfTeacher"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Hi Ramesh..!"
                android:textSize="28sp"
                android:textStyle="bold"
                android:fontFamily="@font/aclonica"
                android:textColor="#FF5722"
                android:shadowColor="#80000000"
                android:shadowDx="2"
                android:shadowDy="2"
                android:shadowRadius="3"
                android:padding="8dp"
                android:layout_below="@id/includeToolbar"
                android:layout_centerHorizontal="true"/>

            <!-- RecyclerView for dashboard items -->
            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/recyclerView"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_below="@id/greetingTextOfTeacher"
                android:layout_margin="10dp"
                android:clipToPadding="false"
                android:paddingBottom="60dp"
                android:scrollbars="none" />

        </RelativeLayout>

        <!-- Floating Action Button -->
        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/fab"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|center"
            android:layout_margin="20dp"
            android:contentDescription="FAB"
            android:src="@drawable/baseline_add_24"/>

    </FrameLayout>

    <!-- Navigation Drawer -->
<!--    <com.google.android.material.navigation.NavigationView-->
<!--        android:id="@+id/navigationView"-->
<!--        android:layout_width="wrap_content"-->
<!--        android:layout_height="match_parent"-->
<!--        android:layout_gravity="start"-->
<!--        app:headerLayout="@layout/nav_header_studentdash"-->
<!--        app:itemIconSize="28sp"-->
<!--        app:itemTextColor="@color/black"-->
<!--        app:menu="@menu/nav_menu_studentdash" />-->

</androidx.drawerlayout.widget.DrawerLayout>
